tags:
  - doc
  - core
  - platform-engineering
related_nodes:
  - docs/en/concept/declarative_ecosystem_integration
  - docs/hu/concept/declarative_ecosystem_integration
  - feature-list
category:
  - concept
entrypoint: true
used_in:
  - onboarding
  - development-process
description: |
  Root README for cic-schema-registry, the consolidated CIC schema registry (general/standards/providers, one schema per file, per-file signing). Entry point for understanding the repository structure and the design it implements.
