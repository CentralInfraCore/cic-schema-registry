---
name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  lint_and_test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker images
        run: make build

      - name: Generate and check requirements.txt for drift
        run: |
          make infra.deps
          git diff --exit-code requirements.txt

      - name: Start development environment
        run: make up

      - name: Run code quality checks
        run: make check

      - name: Run tests and generate coverage report
        run: make test

      - name: Validate schema corpus (inherited meta-schema check)
        run: make validate

      - name: Validate registry (base-chain coverage + version evolution)
        run: make registry.validate

      - name: Upload coverage report artifact
        uses: actions/upload-artifact@v4
        with:
          name: html-coverage-report
          path: htmlcov/

      - name: Stop development environment
        if: always()
        run: make down
